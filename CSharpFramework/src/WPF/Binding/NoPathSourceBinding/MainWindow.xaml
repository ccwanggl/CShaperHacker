<Window
    Height="Auto"
    Title="MainWindow"
    Width="Auto"
    mc:Ignorable="d"
    x:Class="NoPathSourceBinding.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.DataContext>
            <sys:String>
                Hello Grid Data Context
            </sys:String>
        </Grid.DataContext>
        <StackPanel>
            <StackPanel.DataContext>
                <sys:String>
                    Hello Stack panel DataContext!
                </sys:String>
            </StackPanel.DataContext>
            <Grid>
                <StackPanel>
                    <TextBlock
                        Background="Azure"
                        Margin="5"
                        Text="{Binding}" />
                    <TextBlock
                        Background="Beige"
                        Margin="5"
                        Text="{Binding}" />
                    <TextBlock
                        Background="Bisque"
                        Margin="5"
                        Text="{Binding}" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>

</Window>
