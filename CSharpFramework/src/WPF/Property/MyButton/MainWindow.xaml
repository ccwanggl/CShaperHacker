<Window
    x:Class="MyButton.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyButton"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
	<Window.Resources>
		<Style TargetType="{x:Type local:KButton}">
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate>
						<DockPanel
                            Name="dpCon"
                            Width="{Binding Width, RelativeSource={x:Static RelativeSource.TemplatedParent}}"
                            Height="{Binding Height, RelativeSource={x:Static RelativeSource.TemplatedParent}}"
                            Background="{Binding Background, RelativeSource={x:Static RelativeSource.TemplatedParent}}"
                            ToolTip="{Binding ToolTip, RelativeSource={x:Static RelativeSource.TemplatedParent}}">
							<DockPanel Name="dpBtn" DockPanel.Dock="Top">
								<DockPanel.Background>
									<ImageBrush ImageSource="{Binding ForeImage, RelativeSource={x:Static RelativeSource.TemplatedParent}}" Stretch="{Binding Stretch, RelativeSource={x:Static RelativeSource.TemplatedParent}}" />
								</DockPanel.Background>
								<TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="15"
                                    Foreground="#f9fcff"
                                    Text="{Binding Content, RelativeSource={x:Static RelativeSource.TemplatedParent}}" />
							</DockPanel>
						</DockPanel>
						<ControlTemplate.Triggers>
							<DataTrigger Binding="{Binding IsMouseOver, RelativeSource={x:Static RelativeSource.Self}}" Value="True">
								<Setter TargetName="dpBtn" Property="Background">
									<Setter.Value>
										<ImageBrush ImageSource="{Binding BackImage, RelativeSource={x:Static RelativeSource.TemplatedParent}}" Stretch="{Binding Stretch, RelativeSource={x:Static RelativeSource.TemplatedParent}}" />
									</Setter.Value>
								</Setter>
								<Setter TargetName="dpCon" Property="Background" Value="{Binding MouseOverBackColor, RelativeSource={x:Static RelativeSource.TemplatedParent}}" />
							</DataTrigger>
							<DataTrigger Binding="{Binding BackImage, RelativeSource={x:Static RelativeSource.Self}, Mode=TwoWay}" Value="{x:Null}">
								<Setter TargetName="dpBtn" Property="Background">
									<Setter.Value>
										<ImageBrush ImageSource="{Binding ForeImage, RelativeSource={x:Static RelativeSource.TemplatedParent}}" Stretch="{Binding Stretch, RelativeSource={x:Static RelativeSource.TemplatedParent}}" />
									</Setter.Value>
								</Setter>
							</DataTrigger>
							<Trigger Property="IsEnabled" Value="true" />
							<Trigger Property="IsEnabled" Value="false">
								<Setter Property="Foreground" Value="Gray" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Window.Resources>
	<Grid>
		<DockPanel>
			<StackPanel>
				<local:KButton
                    Width="50"
                    Height="50"
                    BackImage="/Image/关闭退出.png"
                    Background="Gray"
                    ForeImage="/Image/关闭.png"
                    MouseOverBackColor="Brown"
                    Stretch="None" />
				<local:KButton
                    Width="50"
                    Height="50"
                    Margin="0,10,0,0"
                    Background="Gray"
                    ForeImage="/Image/关闭.png"
                    MouseOverBackColor="Brown"
                    Stretch="None" />
				<local:KButton
                    Width="100"
                    Height="100"
                    Margin="0,10,0,0"
                    Background="Gray"
                    Content="篮子"
                    ForeImage="/Image/篮子.png"
                    MouseOverBackColor="Brown"
                    Stretch="Fill" />
			</StackPanel>
		</DockPanel>
	</Grid>
</Window>
